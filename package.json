{
  "name": "cuanfans-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler pages dev public --d1=DB --var JWT_SECRET:your-secret-key-here",
    "deploy": "wrangler pages deploy public"
  },
  "dependencies": {
    "hono": "^4.0.0"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
